# Data

Place raw Xente transaction files here:

- `data/raw/data.csv` — main transaction dataset (~95k rows)
- `data/raw/Xente_Variable_Definitions.csv` — field definitions

Raw CSV files are listed in `.gitignore` and should not be committed if your course requires keeping data local.
